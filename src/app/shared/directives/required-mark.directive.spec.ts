import { RequiredMarkDirective } from './required-mark.directive';

describe('RequiredMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredMarkDirective();
    expect(directive).toBeTruthy();
  });
});
